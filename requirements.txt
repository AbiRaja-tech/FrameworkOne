# Essential dependencies for FrameworkOne
# Core dependencies
langgraph>=0.0.20
langchain-core>=0.1.0
python-dotenv>=1.0.0

# OR-Tools for constraint programming
ortools>=9.7.0

# Optional Google services (will use mock data if not available)
gspread>=5.12.0
google-auth>=2.23.0

# Optional ML dependencies (will use mock implementation if not available)
# transformers>=4.35.0
# torch>=2.0.0
# bitsandbytes>=0.41.0

# Development and testing
pytest>=7.0.0